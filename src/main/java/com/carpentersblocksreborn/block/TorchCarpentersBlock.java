package com.carpentersblocksreborn.block;

import net.minecraft.block.TorchBlock;
import net.minecraft.particles.IParticleData;

public class TorchCarpentersBlock extends TorchBlock implements CarpentersBlock {
    public TorchCarpentersBlock(Properties properties, IParticleData particleData) {
        super(properties, particleData);
    }
}
